apiVersion: v1
kind: Service
metadata:
  name: dungeons-and-dragons-service
spec:
  selector:
    app: dungeons-and-dragons
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
